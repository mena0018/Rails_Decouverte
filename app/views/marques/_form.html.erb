<%= form_with(model: marque) do |form| %>
  <% if marque.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(marque.errors.count, "error") %> prohibited this marque from being saved:</h2>

      <ul>
        <% marque.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :libelle %>
    <%= form.text_field :libelle %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
